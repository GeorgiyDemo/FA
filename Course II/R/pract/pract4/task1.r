"
Исследовать и сформулировать отличия в работе функций
определяющих тип вектора: typeof(), mode(), str()
"
w <- c(3,4,4,5)
typeof(w) #Отображение типа объекта
mode(w) #Отображение режима хранения объекта
str(w) #Отображение структуры объекта R
